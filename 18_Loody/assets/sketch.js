
//let w, h, canvas;
//let product_sans_regular, product_sans_bold;
//let logo;
//var mainDiv, titleInput, searchButton,
//let logoImage;

//function preload() {
  // FONTS
  //product_sans_regular = loadFont( 'fonts/Product Sans Regular.ttf' );
  //product_sans_bold = loadFont( 'fonts/Product Sans Bold.ttf' );

  // IMAGES
  //logo = loadImage( 'img/LoodyLogo2.png' );
//}

  // text
  //fill( 0 );
  //textAlign( CENTER );
  //textFont( product_sans_bold );
  //textSize( 40 );
  //text( 'Enter a music title and download it instantly', w / 2.5, 150 );

  //input
  //titleInput = createInput( 'Search...' );
  //titleInput.position( w / 2.5 - titleInput.width / 2 , 180 );


  // buttons
  /*mainDiv = createDiv( '' );
  mainDiv.addClass( 'main-wrapper' );
  searchButton = createButton( '' );
  mainDiv.child( searchButton );
  searchButton.addClass( 'material-icons' );
  searchButton.position( titleInput.x + titleInput.width + 30, 180 );
  searchButton.mousePressed( searchMusic );*/

  // images
  //logoImage = image( logo, 50, 50, 200, 200 );




